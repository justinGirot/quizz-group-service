package com.quizz.group.model;

/**
 * Enum representing the type of group access control.
 */
public enum GroupType {
    /**
     * Anyone can join without invitation
     */
    PUBLIC,

    /**
     * Invitation only membership
     */
    PRIVATE
}
